require 'spec_helper'

describe Activerecord::DbExists do
  it 'has a version number' do
    expect(Activerecord::DbExists::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
